# Self Storage Tools — Desktop App Dependencies

# Claude Agent SDK (used by Fact-Check and Market Analysis agents)
claude-agent-sdk

# Async runtime (required by Agent SDK)
anyio

# Excel file generation
openpyxl

# Distance / geocoding calculations
geopy

# HTTP requests (Google Places / Geocoding API calls)
requests

# Competitor website scraping (handles JS rendering + Cloudflare)
firecrawl-py

# Pricing extraction via Claude API (direct, not Agent SDK)
anthropic

# Environment variable loading from .env
python-dotenv
